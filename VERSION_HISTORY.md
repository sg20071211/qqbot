# 版本更新记录

| 版本 | 内容 |
|------|------|
| **2.3.1** | 虚拟股市修复：修复行情「涨跌」始终显示 0.00% 的 bug（`_make_initial_price_data` 缺少 `prev_close` 键，导致涨跌幅计算基准缺失），初始资金从 1000 提升至 10000，新增 `test_virtual_stock.py` 完整测试脚本覆盖 48 项测试（初始化/消息采集/行情查询/交易指令/用户状态/风控/杠杆/事件/破产恢复/日息/边缘案例） |
| **2.3.0** | 新增虚拟股市板块：`scripts/virtual_stock/` 独立包（9文件~108KB），8支股票×群聊指标定价（群主发言占比/水群频率/关键词匹配/潜水率等），AMM做市商（1%价差），做多/做空/3倍杠杆+爆仓强平，熔断（单股1h涨跌30%或大盘跌15%），拆股（≥1000自动1:10），每周分红，交易体力值（上限10/30min恢复1点），破产恢复，一群一盘数据隔离，8个后台定时协程（股价刷新/爆仓/拆股/体力/日息/富豪榜/收盘/周分红），群主QQ硬编码408754232，帮助系统新增「股市」类别 |
| **2.2.4** | Persona 五段式重构：分离「行为指令」与「事实知识」。角色 persona.txt 改为五段式（核心身份/语言风格/行为准则/情绪反应/互动策略），记忆文件清理去重（knowledge 52→6 条，28KB→7.9KB），新增「查看人设」「设置人设」指令，删除记忆权限逻辑重构，memory_manager 角色创建流程改为五段式模板 + Persona 逐段编辑，画像导入目标修正为群专属记忆 |
| **2.2.3.2** | 新增 requirements.txt，修复 .env.example 与实际代码不匹配，CLAUDE.md 改名为 KUN.md 并完善快速参考文档 |
| **2.2.3** | 修复画像模块低成功率：PROFILE_API_TIMEOUT 20s→40s，移除 _validate_profile 标点结尾校验 |
| **test-agent** | Agent 功能测试：验证 git tag 推送流程 |
| **2.2.2.1** | NapCat UTF-8 启动方式，解决控制台中文乱码 |
| **2.2.2** | 调整周礼模块输出长度：prompt 改为 150-220 字，max_tokens 降至 300，避免超出 QQ 消息发送上限 |
| **2.2.1** | 完善周礼模块日志输出：分离 HTTPStatusError 打印状态码+响应体、补全请求链路日志（开始处理→调用模型→成功/失败→完成）、双模型均失败时明确记录 |
| **2.2.0** | 新增周礼模块：`@bot 周礼 <文字>` 将大白话改写成"合乎周礼"白话翻译腔，AI 自动选择辞气（温言相劝/大儒辩经/强行圆场/痛心疾首），输出 150-260 字，主模型（中科大代理）故障自动切 DeepSeek 官方 |
| **2.1.0** | 多群多角色多记忆：群→角色映射（group_characters.json）、群专属记忆（memories/group/）、角色通用记忆（memories/character/）、别名覆盖所有角色、查看记忆改为发送文件 |
| **2.0.1** | API Key 脱敏：9 个文件 10 处硬编码 Key 迁移至 .env 环境变量，新增 .env.example 模板，初始化 Git 仓库并推送到 GitHub |
| **2.0.0** | 指令逻辑从 `reverse_bot.py` 拆出到 `scripts/command_handler.py`，`reverse_bot.py` 从 2395 行精简至 478 行，实现指令与核心逻辑完全分离 |
| 1.14.0 | 新增 Pixiv 排行榜随机图片（日榜/周榜/月榜，PHPSESSID 鉴权） |
| 1.13.0 | 新增角色名字/别名匹配（角色模式下，消息含角色名视为 @bot） |
| 1.12.0 | 新增猫娘图片（Nekosia API） + 神秘数字（JMComic ID 独立命令） |
| 1.11.0 | 新增签到功能（每日签到 + 排名 + 连续天数） |
| 1.10.4 | 修复中文拒绝语过滤，备用模型也走完整内容校验 |
| 1.10.3 | 新增 `@bot test` 命令：测试主模型和备用模型 API 连通性 |
| 1.10.2 | 启动性能优化：分类器按需导入，画像更新延迟至 5 分钟、频率降至 2 小时 |
| 1.10.1 | 修复熔断 fallback 密钥错误：新增 `DecisionEngine.fallback_api_key` 参数 |
| 1.10.0 | 移除守护进程依赖（monitor_napcat.py / napcat_guardian.py / 定时关机），下线恢复改为手动远程 |
| 1.9.0 | 新增模型切换命令（切换模型/查看模型） + 角色记忆管理（添加/删除/查看记忆、切换/启用/停用角色） |
| 1.8.0 | 新增语音转写（NapCat HTTP API 语音 → 文字） |
| 1.7.0 | 新增分层帮助系统（`帮助` → 类别概览，`[类别]帮助` → 详细命令列表） |
| 1.6.0 | 新增自主发言决策引擎（上下文驱动智能回复，子进程隔离 + 多模型故障转移 + 零样本分类器 + 消息密度/冷却检查） + 用户画像系统（缓冲发言 + 定时 API 总结 + 注入决策上下文），机器人从此不再依赖 @ 触发即可自主判断是否发言 |
| 1.5.0 | 新增钢琴功能（SimPiano 曲谱解析 + numpy 钢琴音频合成 + WAV 发送） |
| 1.4.0 | 新增像素画功能（图片 → 32×32 像素预览 + LaTeX 表格文件） |
| 1.3.0 | 新增算卦/占卜（梅花易数起卦，基于用户 QQ 号/昵称/时间生成卦象 + DeepSeek AI 解读吉凶） |
| 1.2.0 | 新增加密/解密（咕嘎密文，比特级编码算法） |
| 1.1.0 | 新增 Yau（丘成桐风格 AI 对话，调用 DeepSeek API 模拟数学家说话风格，每小时限 3 次） |
| 1.0.2 | 新增 `+1` 复读检测 + 新成员入群欢迎通知 |
| 1.0.1 | 新增上线问候 + 入群指引 + 定时关机（22:00 自动退出） |
| 1.0.0 | 初始版本：菜单系统（添加/删除/查看菜单、今天吃什么、选择），数据存储于 `menu_data.json` |







---

## 虚拟股版本

| 版本 | 内容 |
|------|------|
| **0.1.0** | 🚧 开发中 — 初始架构搭建 |

---

## 工具版本

| 版本 | 工具 | 内容 |
|------|------|------|
| **0.0.1** | group_manager | 群功能管理器：按群启用/禁用决策引擎、+1 复读、指令响应、新人欢迎、用户画像五项功能，支持从 user_profiles.json 自动发现群号 |